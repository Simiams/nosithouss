spring:
  profiles:
    active: default
  main:
    web-application-type: servlet
    etl: true
  datasource:
    url: jdbc:postgresql://localhost/arosaje
    username: nosithouss
    password: nosithouss
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:migrations
  security:
    user:
      name: nosithouss
      password: nosithouss

debug: false

logging:
  level:
    web: DEBUG