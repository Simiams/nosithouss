export enum choiceSearch {
  USER = "Personne",
  CATALOG = "Catalog",
  POST = "Post",
  GUARDINGPOST = "Guarde de plante"
}


export enum EPostType {
  CATALOG,
  GUARDING,
  POST
}

export enum EPostTypeStr {
  CATALOG = "CATALOG",
  GUARDING = "GUARDING",
  POST = "POST"
}
